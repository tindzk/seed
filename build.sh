#!/bin/sh
set -x
curl -L -o csbt https://github.com/coursier/sbt-launcher/releases/download/v1.2.5/csbt
chmod +x csbt
./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile
