#!/bin/sh
set -x
curl -o csbt https://raw.githubusercontent.com/coursier/sbt-launcher/master/csbt
chmod +x csbt
./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile || \
	 ./csbt compile
